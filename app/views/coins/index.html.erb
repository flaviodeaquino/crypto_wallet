<p style="color: green"><%= notice %></p>

<h1>Moedas</h1>

<div id="coins">
  <% @coins.each do |coin| %>
    <%= render coin %>
    <p>
      <%= link_to t('links.show'), coin %>
    </p>
  <% end %>
</div>

<%= link_to "Novo registro de moeda", new_coin_path %>
